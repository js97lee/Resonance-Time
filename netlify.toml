# Netlify: main 푸시 시 자동 빌드 (대시보드에서 GitHub 저장소 연결 필요)
# 현재 빌드 산출물은 docs/로 출력됨 (GitHub Pages main/docs 전략과 동일)

[build]
  command = "npm run build"
  publish = "docs"

[build.environment]
  NODE_VERSION = "20"

# React Router 클라이언트 라우트
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
